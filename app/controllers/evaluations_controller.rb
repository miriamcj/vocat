class EvaluationsController < ApplicationController

  respond_to :json

  # GET /exhibit/1.json
  def show
#		@exhibit = Exhibit.factory({:id => params[:id], :viewer => current_user})
#		respond_with @exhibit.as_json({})
  end

end
