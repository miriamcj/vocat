<h3>Editing project:</h3>
<h4><%= @project %></h4>

<%= simple_form_for(@project, :url => admin_course_project_path(@course, @project)) do |f| %>
  <%= render 'form', :f => f %>
<% end %>

<%= link_to 'Back', admin_course_path(@course), :class => 'btn' %>
