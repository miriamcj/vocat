<% content_for :course_selector do %>
  <%= collection_select(
      :current_course, :id, current_user.creator_courses, :id, :to_s, {:include_blank => "All of my courses"},
      :id => "course_select", :data => {:url => creator_course_overview_path(@organization.id, current_user.courses.first)}
  ) %>
<% end %>

<% content_for :adaptive_navigation do %>
<nav class="nav-adaptive">
  <div class="container">
    <ul class="nav">
      <li><a class="active" href="#">My Presentations</a></li>
      <li><a href="#">Evaluate Peer Presentations</a></li>
      <li><a href="#">Feedback</a></li>
    </ul>
  </div>
</nav>
<% end %>

<%= render :template => 'layouts/application' %>