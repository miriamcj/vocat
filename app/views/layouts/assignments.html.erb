<% content_for :course_selector do %>
  <% if @course %>
    <%= collection_select :course, :id, @courses, :id, :to_s, {}, :class => "span4", :id => "course_select", :selected => @course.id, :data => {:url => organization_course_assignments_path(@organization, @course)} %>
  <% end %>
<% end %>
<%= render :template => 'layouts/application' %>