<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
    <div class="alert-<%= name == :notice ? "success" : "error" %>">
      <%= content_tag :p, msg, :id => "flash_#{name}" %>
      <a class="alert--close" href="#">&#10683;</a>
    </div>
  <% end %>
<% end %>
