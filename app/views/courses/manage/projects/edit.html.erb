<% content_for :content_header do %>
  Edit Project
<% end %>

<% content_for :content_header_links do %>
  <%= link_to 'Back', course_manage_projects_path(@course), :class => 'gray-ex-button' %>
<% end %>

<% content_for :content_intro do %>
  <p class="bottom-flush">
    Project editing instructions go here...
  </p>
<% end %>

<% content_for :content_frames do %>
  <div class="page-content--frame">
    <%= simple_form_for(@project, :url => course_manage_project_path(@course, @project)) do |f| %>
      <%= render 'form', :f => f %>
      <div class="input-group">
        <%= link_to 'Back', course_manage_projects_path(@course), :class => 'gray-ex-button' %>
        <%= f.button :submit, 'Save project', :class => 'teal-plus-button' %>
      </div>
    <% end %>
  </div>
<% end %>
