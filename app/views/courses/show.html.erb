<% if can? :update, @course %>
  <div><%= link_to 'Edit', edit_organization_course_path(@organization, @course) %></div>
<% end %>

<% if can? :destroy, @course %>
  <div><%= link_to 'Destroy', organization_course_path(@organization, @course), method: :delete, data: { confirm: 'Are you sure?' } %></div>
<% end %>
<p class="lead">In this course you are a <%= @course.role(current_user) %>.</p>

<p>
  <b>Name:</b>
  <%= @course.name %>
</p>

<p>
  <b>Department:</b>
  <%= @course.department %>
</p>

<p>
  <b>Number:</b>
  <%= @course.number %>
</p>

<p>
  <b>Section:</b>
  <%= @course.section %>
</p>

<p>
  <b>Description:</b>
  <%= @course.description %>
</p>

<div>
  <b>Students:</b>
  <ul>
    <% @course.students.each do |student| %>
      <li><%= student.name %></li>
    <% end %>
  </ul>
</div>

<div>
  <b>Helpers:</b>
  <ul>
    <% @course.helpers.each do |helper| %>
      <li><%= helper.name %></li>
    <% end %>
  </ul>
</div>

<div>
  <b>Instructors:</b>
  <ul>
    <% @course.instructors.each do |instructor| %>
      <li><%= instructor.name %></li>
    <% end %>
  </ul>
</div>


