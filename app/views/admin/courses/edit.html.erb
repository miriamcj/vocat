<%= render :partial => 'heading', :locals => {course: @course, subheader: 'Edit Course Properties'} %>

<% content_for :content do %>

  <% body = capture do %>
    <%= form_for [:admin, @course], :html => {:class => 'formgrid'} do |f| %>
      <fieldset>
        <div class="formgrid--messaging">
          <%= render 'shared/model_errors', :model => @course %>
        </div>
        <%= render 'form', :f => f %>
        <div class="formgrid whole">
          <%= f.button 'Update Course', :class => 'primary-button new-button' %>
          <%= link_to 'Back', admin_course_path(@course), :class => 'secondary-button' %>
        </div>
      </fieldset>
    <% end %>
  <% end %>
  <%= render :partial => 'shared/section', :locals => {:header => nil, :body => body} %>
<% end %>

