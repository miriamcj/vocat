<%= form_for(@assignment_submission) do |f| %>
  <% if @assignment_submission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assignment_submission.errors.count, "error") %> prohibited this assignment_submission from being saved:</h2>

      <ul>
      <% @assignment_submission.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :summary %><br />
    <%= f.text_area :summary %>
  </div>
  <div class="actions">
    <%= f.submit "Continue" %>
  </div>
<% end %>

