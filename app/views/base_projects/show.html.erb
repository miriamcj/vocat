<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @project.name %>
</p>

<p>
  <b>Description:</b>
  <%= @project.description %>
</p>

<p>
  <b>Course:</b>
  <%= @project.course %>
</p>

<p>
  <b>Project type:</b>
  <%= @project.project_type %>
</p>

<% if can? :update, @project %>
  <%= link_to 'Edit', edit_organization_course_project_path(@organization, @course, @project) %> |
<% end %>
<%= link_to 'Back', organization_course_projects_path(@organization, @course)%>
