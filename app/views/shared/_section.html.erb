<% clear = false if local_assigns[:clear].nil? %>

<div class="page-section <% if clear %>page-section-clear<% end %>">
  <% if header %>
  <div class="header-wrapper">
    <div class="header">
      <h2 class="heading-page-section"><%= header %></h2>
    </div>
  </div>
  <% end %>
  <div class="body">
    <%= body %>
  </div>
</div>