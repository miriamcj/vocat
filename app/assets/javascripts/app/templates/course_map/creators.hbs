    <thead>
      <th>
        <!--
          <label class="light-radio-label" data-behavior="show-users">
            <input type="radio" name="meta-nav-toggle" {{#if isUsers}}checked="checked"{{/if}}>
            <b></b>
            <span class="light-radio-label--text">Students</span>
          </label>
          <label class="light-radio-label" data-behavior="show-groups">
            <input type="radio" name="meta-nav-toggle" {{#if isGroups}}checked="checked"{{/if}}>
            <b></b>
            <span class="light-radio-label--text">Groups</span>
          </label>
        -->
      </th>
    </thead>
    <tbody data-behavior="matrix-creators">
      <!-- creators_item.hbs -->
    </tbody>

